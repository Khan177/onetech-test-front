import React from "react";

import {
  Dialog,
  DialogActions,
  DialogContent,
  DialogContentText,
  DialogTitle,
} from "@material-ui/core";

const DialogComponent = () => {
  return (
    <Dialog open={true}>
      <DialogTitle>he</DialogTitle>
    </Dialog>
  );
};

export default DialogComponent;
